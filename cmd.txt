docker exec -it spark-master \
  spark-submit \
  --master spark://spark-master:7077 \
  /opt/spark/apps/load_bronze.py

docker exec -i openldap sh -c 'cat | ldapadd -x -H ldap://localhost:389 -D "cn=admin,dc=example,dc=org" -w admin' <<'LDIF'
dn: ou=people,dc=example,dc=org
objectClass: organizationalUnit
ou: people

dn: uid=hung,ou=people,dc=example,dc=org
objectClass: inetOrgPerson
uid: hung
sn: hung
cn: hung
userPassword: hung
LDIF